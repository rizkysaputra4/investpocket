import { BsNavbarBtnDirective } from './bs-navbar-btn.directive';

describe('BsNavbarBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new BsNavbarBtnDirective();
    expect(directive).toBeTruthy();
  });
});
