import { NsNavbarDirective } from './ns-navbar.directive';

describe('NsNavbarDirective', () => {
  it('should create an instance', () => {
    const directive = new NsNavbarDirective();
    expect(directive).toBeTruthy();
  });
});
