import { PricePocketPipe } from './price-pocket.pipe';

describe('PricePocketPipe', () => {
  it('create an instance', () => {
    const pipe = new PricePocketPipe();
    expect(pipe).toBeTruthy();
  });
});
