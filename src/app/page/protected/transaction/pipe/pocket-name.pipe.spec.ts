import { PocketNamePipe } from './pocket-name.pipe';

describe('PocketNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PocketNamePipe();
    expect(pipe).toBeTruthy();
  });
});
